{
  "id": "US-AUTH-PERSIST",
  "title": "Fix Auth Session Persistence",
  "description": "After clicking magic link, user should stay logged in. Currently session doesn't persist - user is prompted to login again when navigating.",
  "priority": "HIGH",
  "technicalContext": {
    "symptoms": [
      "Click magic link -> redirected but not logged in",
      "Navigate to /dashboard -> prompted to login",
      "Header doesn't show Dashboard/Sign Out buttons",
      "Session cookie not being set or read correctly"
    ],
    "relatedFiles": [
      "convex/betterAuth.ts - session handling",
      "src/lib/auth-client.ts - client-side auth",
      "src/components/Header.tsx - auth-aware nav"
    ]
  },
  "acceptanceCriteria": [
    {"text": "Investigate why session cookies aren't persisting after magic link verification", "checked": true},
    {"text": "Check Better Auth session configuration (cookie settings, secure, sameSite)", "checked": true},
    {"text": "Verify CORS allows credentials (Access-Control-Allow-Credentials)", "checked": true},
    {"text": "After clicking magic link, user lands on /dashboard AND stays logged in", "checked": true},
    {"text": "Header shows 'Dashboard' and 'Sign Out' buttons when logged in", "checked": true},
    {"text": "Refreshing page maintains logged-in state", "checked": true},
    {"text": "Navigating between pages maintains logged-in state", "checked": true},
    {"text": "Sign Out button works and clears session", "checked": true},
    {"text": "Typecheck passes", "checked": true},
    {"text": "All unit tests pass", "checked": true}
  ],
  "passes": true,
  "implementationNotes": "Implemented in feature/auth-fixes worktree. Added crossDomainClient() plugin to auth-client.ts which handles localStorage-based session persistence for cross-domain auth (frontend at :3001, auth at convex.site). CORS credentials already handled internally by @convex-dev/better-auth."
}
