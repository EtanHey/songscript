{
  "id": "SEC-005",
  "title": "Secure userWishlist.ts and userGoals.ts",
  "description": "Apply the same ctx.auth pattern to userWishlist.ts and userGoals.ts. Remove all visitorId arguments.",
  "acceptanceCriteria": [
    {"text": "Import getAuthUserId, requireAuth from authHelpers in both files", "checked": false},
    {"text": "userWishlist: Remove visitorId from all args", "checked": false},
    {"text": "userWishlist: Use by_user index for all queries", "checked": false},
    {"text": "userWishlist: Use requireAuth for mutations", "checked": false},
    {"text": "userGoals: Remove visitorId from all args", "checked": false},
    {"text": "userGoals: Use by_user index for all queries", "checked": false},
    {"text": "userGoals: Use requireAuth for mutations", "checked": false},
    {"text": "Typecheck passes", "checked": false}
  ],
  "passes": false,
  "blockedBy": "SEC-002",
  "notes": "Follow the EXACT same pattern as SEC-003 (wordProgress.ts):\n\n1. Import helpers: `import { getAuthUserId, requireAuth } from './authHelpers'`\n2. Remove visitorId from args: `args: {}` or only non-user args\n3. Get userId inside handler: `const userId = await requireAuth(ctx)`\n4. Query by userId: `.withIndex('by_user', q => q.eq('userId', userId))`"
}
