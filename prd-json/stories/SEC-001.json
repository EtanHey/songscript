{
  "id": "SEC-001",
  "title": "Create Convex auth helper and save reference docs",
  "description": "Create convex/authHelpers.ts with secure auth patterns. Save Context7 docs locally for reference. This establishes THE pattern all other stories must follow.",
  "acceptanceCriteria": [
    {"text": "Create docs.local/convex-auth-patterns.md with Context7 best practices", "checked": true},
    {"text": "Create convex/authHelpers.ts with getAuthUserId(ctx) helper", "checked": true},
    {"text": "getAuthUserId returns userId from authComponent.safeGetAuthUser(ctx)", "checked": true},
    {"text": "getAuthUserId returns null if not authenticated (NOT throw)", "checked": true},
    {"text": "Add requireAuth(ctx) helper that throws if not authenticated", "checked": true},
    {"text": "Add JSDoc comments explaining security rationale", "checked": true},
    {"text": "Typecheck passes", "checked": true}
  ],
  "passes": true,
  "blockedBy": null,
  "completedAt": "2026-01-21T21:00:00Z",
  "notes": "CRITICAL SECURITY PATTERN:\n\n```typescript\n// convex/authHelpers.ts\nimport { authComponent } from './betterAuth';\n\n// Returns userId or null - use for optional auth\nexport async function getAuthUserId(ctx: any): Promise<string | null> {\n  const authUser = await authComponent.safeGetAuthUser(ctx);\n  return authUser?._id ?? null;\n}\n\n// Throws if not authenticated - use for required auth\nexport async function requireAuth(ctx: any): Promise<string> {\n  const userId = await getAuthUserId(ctx);\n  if (!userId) throw new Error('Authentication required');\n  return userId;\n}\n```\n\nWHY THIS PATTERN:\n- ctx.auth is cryptographically verified, CANNOT be spoofed\n- NEVER accept userId as function argument (can be faked by attacker)\n- Use authComponent.safeGetAuthUser() for Better Auth integration\n\nUse Context7 MCP to fetch latest Convex + Better Auth docs and save to docs.local/"
}
