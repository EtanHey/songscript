{
  "id": "SEC-010",
  "title": "Update auth verify flow to handle migration and display name",
  "description": "Update the magic link verification flow to: check migration preference, run migration if opted in, set display name, clear localStorage.",
  "acceptanceCriteria": [
    {"text": "Update src/routes/auth/verify.tsx to check songscript_migrate_on_signup", "checked": false},
    {"text": "If migrate=true, call migrateAnonymousData mutation with visitorId", "checked": false},
    {"text": "Check songscript_signup_display_name and set if present", "checked": false},
    {"text": "Clear localStorage keys after successful migration: songscript_visitor_id, songscript_migrate_on_signup, songscript_signup_display_name", "checked": false},
    {"text": "Keep songscript_welcome_shown (not user-specific)", "checked": false},
    {"text": "Show migration success message if data was migrated", "checked": false},
    {"text": "Redirect to dashboard after completion", "checked": false},
    {"text": "Handle migration errors gracefully (still complete login)", "checked": false},
    {"text": "Typecheck passes", "checked": false},
    {"text": "Verify in browser: full signup flow works", "checked": false}
  ],
  "passes": false,
  "blockedBy": "SEC-009",
  "notes": "Flow after magic link click:\n\n1. User clicks magic link → arrives at /auth/verify\n2. Better Auth verifies token, creates session\n3. Check localStorage for migration preference:\n   ```typescript\n   const shouldMigrate = localStorage.getItem('songscript_migrate_on_signup') === 'true';\n   const visitorId = localStorage.getItem('songscript_visitor_id');\n   const displayName = localStorage.getItem('songscript_signup_display_name');\n   ```\n4. If shouldMigrate && visitorId:\n   ```typescript\n   const result = await migrateAnonymousData({ visitorId });\n   console.log('Migrated:', result);\n   ```\n5. If displayName:\n   ```typescript\n   await setDisplayName({ displayName });\n   ```\n6. Clear localStorage:\n   ```typescript\n   localStorage.removeItem('songscript_visitor_id');\n   localStorage.removeItem('songscript_migrate_on_signup');\n   localStorage.removeItem('songscript_signup_display_name');\n   ```\n7. Redirect to /dashboard"
}
