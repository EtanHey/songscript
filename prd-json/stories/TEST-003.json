{
  "id": "TEST-003",
  "title": "E2E: Protected routes redirect unauthenticated users",
  "description": "Playwright e2e test verifying that authenticated routes properly redirect to login and that public routes remain accessible.",
  "acceptanceCriteria": [
    {"text": "Create e2e/auth-routes.spec.ts", "checked": false},
    {"text": "Test: /dashboard redirects to /login when not authenticated", "checked": false},
    {"text": "Test: /settings redirects to /login when not authenticated", "checked": false},
    {"text": "Test: /leaderboard redirects to /login when not authenticated", "checked": false},
    {"text": "Test: / (home) is accessible without auth", "checked": false},
    {"text": "Test: /song/:id is accessible without auth (public song pages)", "checked": false},
    {"text": "Test: /signup is accessible without auth", "checked": false},
    {"text": "Test: /login is accessible without auth", "checked": false},
    {"text": "Test: After login, user can access /dashboard", "checked": false},
    {"text": "All tests pass: bun run test:e2e --grep 'auth-routes'", "checked": false},
    {"text": "Typecheck passes", "checked": false}
  ],
  "passes": false,
  "blockedBy": "SEC-010",
  "notes": "Verifies the _authed route group is working correctly.\n\nTest structure:\n```typescript\ntest.describe('Auth Routes', () => {\n  test.describe('Unauthenticated', () => {\n    test('dashboard redirects to login', async ({ page }) => {\n      await page.goto('/dashboard');\n      await expect(page).toHaveURL(/\\/login/);\n    });\n  });\n\n  test.describe('Public routes', () => {\n    test('home page is accessible', async ({ page }) => {\n      await page.goto('/');\n      await expect(page).toHaveURL('/');\n      await expect(page.getByText('SongScript')).toBeVisible();\n    });\n  });\n});\n```"
}
